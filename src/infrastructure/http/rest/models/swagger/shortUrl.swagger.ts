/**
 * @swagger
 * components:
 *   schemas:
 *     shortUrl:
 *       type: object
 *       properties:
 *         shortUrlId:
 *           type: string
 *         code:
 *           type: string
 *         originalUrl:
 *           type: string
 *         url:
 *           type: string
 *         createdAt:
 *           type: date
 *         updatedAt:
 *           type: date
 *         deletedAt:
 *           type: date
 */